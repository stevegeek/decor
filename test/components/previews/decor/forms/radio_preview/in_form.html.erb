<%= render ::Decor::Forms::Form.new(model: model, url: "#", local: true) do |form_component| %>
  <%= render ::Decor::Forms::LayoutContainer.new do |container| %>
    <%= render ::Decor::Forms::LayoutSection.new(title: "Radio", stacked: stacked_form) do %>
      <%= form_component.builder.radio_button :chosen,
                         "true",
                         label: "One",
                         description: "Description for one" %>
      <%= form_component.builder.radio_button :chosen,
                         "true",
                         label: "Two" %>
    <% end %>
    <% container.with_buttons do %>
      <%= form_component.builder.submit %>
    <% end %>
  <% end %>
<% end %>
