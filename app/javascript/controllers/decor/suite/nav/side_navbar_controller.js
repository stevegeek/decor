import SideNavbarBaseController from "../../nav/side_navbar_base";

// Suite skin's SideNavbar controller — its own class (registered under the
// Suite identifier), extending the shared skin-neutral base. No dependency on
// the Daisy skin. Override here for Suite-specific behaviour.
export default class extends SideNavbarBaseController {}
